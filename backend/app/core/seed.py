"""
PMI中国AI项目管理社区 - 种子数据（已停用）

数据真实性策略：系统不再自动写入任何演示/示例数据，保证生产环境所有数据均为用户真实数据。
若需初始演示数据用于本地开发/演示，请显式运行开发用脚本 scripts/seed_data.py（仅开发/演示环境使用，严禁在生产执行）。
"""
import logging

logger = logging.getLogger("app.seed")


async def ensure_seed_data() -> None:
    """保留接口兼容性，但不再写入任何演示/示例数据（数据真实性策略）。

    历史实现会在系统无任何项目时自动插入「PMI中国 AI-PM 示例项目」及若干任务，
    现统一停用，确保生产数据库仅包含用户真实产生的数据。
    """
    logger.info("自动演示数据写入已停用（数据真实性策略），跳过。")
